# Версия файла: 1.1.0
# Описание: Nginx vhost для mpsellerbot.kuzkabuh.ru (HTTP->HTTPS, ACME webroot, webhook proxy, /health)
# Дата изменения: 2025-12-28

server {
    listen 80;
    listen [::]:80;

    server_name mpsellerbot.kuzkabuh.ru;

    # ACME challenge (certbot webroot)
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
        try_files $uri =404;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;

    server_name mpsellerbot.kuzkabuh.ru;

    # Сертификаты Let’s Encrypt (будут появляться после первичного certbot certonly)
    ssl_certificate     /etc/letsencrypt/live/mpsellerbot.kuzkabuh.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/mpsellerbot.kuzkabuh.ru/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;

    client_max_body_size 10m;

    # Health-check наружу
    location = /health {
        proxy_pass http://bot:8000/health;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    # Webhook (секретный путь /webhook/<secret>)
    location ^~ /webhook/ {
        proxy_pass http://bot:8000;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }

    # На случай если секрет не задан и используется /webhook
    location = /webhook {
        proxy_pass http://bot:8000/webhook;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }

    location / {
        return 404;
    }
}
