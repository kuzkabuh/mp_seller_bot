# Telegram
BOT_TOKEN=PUT_YOUR_TELEGRAM_BOT_TOKEN_HERE

# Encryption (Fernet key 32 urlsafe base64 bytes)
# Generate in python: from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())
FERNET_KEY=PUT_YOUR_FERNET_KEY_HERE

# Database
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=mp_seller_bot
POSTGRES_USER=mpbot
POSTGRES_PASSWORD=mpbot_password

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Polling interval (seconds)
POLL_INTERVAL_SECONDS=60

# Log level
LOG_LEVEL=INFO
